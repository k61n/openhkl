Trypsin-Daten / Biodiff2500
LoadData -> new.hdf einlesen
unter Data > new anhaken
new doppelklicken -> Bilder erscheinen
vertikaler Slider -> Konrast, Peaks werden sichtbar

Data rechtsklicken > Find Peaks -> Fenster 'Peak finder' geht auf
- "apply threshold to preview" anhaken
- Threshold = 60-80
- r3 = 20
.. searches (progress bar)

Peaks > Found peaks anhaken
Options > Peak > "Draw peak integration area" anhaken
grün: Integration, gelb: Untergrund

Peaks rechtsklicken > Filter Peaks
  Optionen 4-7 anhaken
  Remove with sigma less than = 5.5
  [ Remove peaks with d less than = 1.5 ??]
  [ Remove peaks with d greater than = 50 ??]

Peaks rechtsklicken > FFT-autoindex
  Max. cell dimension = 100 (Vorwissen)
  NICHT OK drücken, sondern "Autoindex"
  Korrekte Einheitszelle doppelklicken

BioDiff > Sample > UnitCell
  Spacegroup angeben

Peaks rechtsklicken > Integrate Peaks
  (integriert schon gefundene Peaks)
  Fenster "Reintegrate peaks"
  Method = 1d oder 3d
  (könnte schon I(hkl) erzeugen,
   Konsistenztest, nicht für Produktion)

Peaks > "Refine Lattice"
  (u.a. Einfallsrichtung; verbessert Einheitszelle ...)

Peaks > "Build shape library"

Peaks > "Integrate peaks"
