
# linux only!!
add_custom_target(gsl_library)

set(GSL_LIBRARIES ${CMAKE_CURRENT_BINARY_DIR}/.libs/libgsl.a)
set(GSL_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR})

add_custom_command(TARGET gsl_library
  COMMAND ${CMAKE_CURRENT_SOURCE_DIR}/configure
  COMMAND make
  )

