variables:
  DEBIAN_FRONTEND: noninteractive

image: iffregistry.fz-juelich.de/neutron-simlab/openhkl/rocky9-env

stages:
  - build
  - test
  - deploy:doc

build:rocky9:
  stage: build
  script:
    - mkdir build
    - cd build
    - |
      if test $CI_PIPELINE_SOURCE == "nightly"
      then
          cmake .. -D OHKL_FULL_WORKFLOW_TEST=ON
      else
          cmake ..
      fi
    - make -j8
  artifacts:
    paths:
      - build
    expire_in: 1 day

test:rocky9:
  stage: test
  script:
    - cd build
    - |
      if test $CI_PIPELINE_SOURCE == "nightly"
      then
          ctest --output-on-failure -L "nightly"
      else
          ctest -j8 --output-on-failure -E "nightly"
      fi

pages:
  stage: deploy:doc
  script:
    - cd build
    - cmake ..
    - make docs
    - mv doc/sphinx/html ../public/
    - mv doc/sphinx/_static/* ../public/_static/
  artifacts:
    paths:
      - public
  only:
    - main
