stages:
  - build
  
build:
  stage: build
  script:
    - mkdir ./build
    - cmake -DENABLE_CPACK=ON  -DBUILD_QTAPPS=OFF
    - make -j2 -l2
    - make DESTDIR=./build install
    - ctest -j2 -l2
    - make DESTDIR=./build package

  only:
    - master
  tags:
    - ubuntu14.04